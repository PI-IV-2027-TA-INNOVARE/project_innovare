# P&D Connect - backend
#
# Nenhuma dependencia nova em relacao ao backend legado: este arquivo e um
# subconjunto do que ja estava em uso. Acrescentar qualquer linha aqui e portao
# de decisao (AGENTS.md 3.2) e precisa de aprovacao humana.

# --- Nucleo ------------------------------------------------------------------
Django==5.2.14
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
drf-spectacular==0.29.0
psycopg2-binary==2.9.11
python-decouple==3.8

# --- Chegam com as fases de IA e assincrono ----------------------------------
# Fase 3.2 (Copiloto) e 3.3 (matching). Descomente quando a fase comecar - ate
# la nao ha por que instalar torch em maquina de quem so mexe em cadastro.
#
# pgvector==0.4.2
# sentence-transformers==5.5.0
# celery==5.5.3
# redis==6.1.1
