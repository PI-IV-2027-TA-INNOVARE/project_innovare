from django.apps import AppConfig


class MaturityConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.maturity'
    label = 'maturity'
    verbose_name = 'Pre-analise de maturidade'
